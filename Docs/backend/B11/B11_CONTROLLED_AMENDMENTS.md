# B11 — Controlled Amendments

> Every item below requires CTO approval **before implementation**; B11 applies none of them and edits no frozen file. `NON_ADDITIVE_AMENDMENTS = 0`.

| # | AMENDMENT_ID | FROZEN_SOURCE | FROZEN_TEXT/SEMANTIC | B11_REQUIREMENT | CLASSIFICATION | BLAST_RADIUS | REQUIRED_UPDATES |
|---|---|---|---|---|---|---|---|
| 1 | `B11-AM-001` | `BACKEND_OPENAPI_V1.yaml`, `BACKEND_DTO_CONTRACTS.md` | `UploadRequest` = `{filename, content_type}`, both required, `additionalProperties: false`. `FileAsset` = `{public_id, filename, content_type, size_bytes?}`, first three required, `additionalProperties: false` | Add **optional** properties only. `UploadRequest`: `size_bytes?`, `sha256?`, `retention_class?` (system-actors only). `FileAsset`: `status?`, `checksum?` (**explicitly nullable**), `created_at?`, `updated_at?`, `version?`, `upload?` (`{ticket, expires_at, max_bytes}` — never a provider URL or credential). Every currently-required field stays required; **`content_type` in particular stays required, stays `string`, and is never nullable** — it is `NOT NULL` in `file_assets` from insert and carries the validated provisional type while `pending` (`B11_FILE_VALIDATION.md` §3.1), so no `FileAsset` response in any lifecycle state can omit or null it. The frozen-optional `size_bytes` is **omitted** while unmeasured rather than nulled, because the frozen schema types it `integer`. No field is removed, renamed, retyped, or made optional | `ADDITIVE` | two schema objects gain optional properties. A client written against the frozen shape continues to work: it sends no new request field, receives every field it requires with the type it expects, and ignores new response fields. `additionalProperties: false` still holds — the closed set is simply larger. **Nothing in this amendment relaxes a frozen requirement, so no `NON_ADDITIVE` companion is needed or filed** | `BACKEND_OPENAPI_V1.yaml` `UploadRequest`/`FileAsset`; `BACKEND_DTO_CONTRACTS.md` §"B0-FIX.1 transport DTO index" row |
| 2 | `B11-AM-002` | `BACKEND_API_CATALOG.md`, `BACKEND_OPENAPI_V1.yaml` | exactly two Files operations exist: `POST /api/v1/files/uploads` and `GET /api/v1/files/{id}/download` | Add the nine operations in `B11_API_DTO_CONTRACTS.md` §1 (rows 2, 3, 4, 6, 7, 8, 9, 10, 11). Both frozen operations are unchanged in path, method, `operationId`, permission, request schema, success schema, and **complete declared response set** — including `createFileUpload`'s frozen `502 ProviderUnavailable`, which B11 **retains verbatim** even though the Phase-1 metadata-only path cannot raise it (`B11_API_DTO_CONTRACTS.md` §5). No frozen response is removed, narrowed, or re-pointed | `ADDITIVE` | nine new catalog rows and nine new OpenAPI path items. **No `filters`/`sort` marker is added** — B11 introduces no collection endpoint, so the frozen explicit filtering allow-list is untouched | `BACKEND_API_CATALOG.md` endpoint table; `BACKEND_OPENAPI_V1.yaml` paths |
| 3 | `B11-AM-003` | `BACKEND_API_CATALOG.md` lines 38-39 vs. `BACKEND_OPENAPI_V1.yaml` | The catalog calls `POST /files/uploads` "signed upload" and gives `GET /files/{id}/download` the DTO "redirect/stream"; the OpenAPI gives the first a `FileAsset` body carrying no URL and the second a `FileDownload{download_url, expires_at}` JSON body. The two frozen artifacts under-specify, and partly disagree about, what "signed" and "stream" mean | Record the resolution: (a) "signed upload" is an **application-signed, short-lived, single-use upload authorization** returned in the additive `FileAsset.upload.ticket`, never a provider pre-signed URL in Phase 1; (b) `/download` returns the frozen `FileDownload` JSON, whose `download_url` is an **application URL on WazLink's own origin** pointing at the additive `GET /files/{id}/content`, which is the "stream" the catalog names. Both frozen descriptions become simultaneously true; neither is contradicted | `COMPATIBLE_CLARIFICATION` | no schema, path, method, or status changes. Two prose cells in the catalog gain a precise meaning they did not previously carry. Same class as `B10-AM-003`'s treatment of a frozen name that was never bound to a shipped schema | `BACKEND_API_CATALOG.md` Files rows |
| 4 | `B11-AM-004` | `BACKEND_ERROR_CATALOG.md` | the closed code list includes `FILE_TYPE_NOT_ALLOWED` (422); no other file-lifecycle code exists | Add three `code` values: `FILE_TOO_LARGE` (422), `FILE_INTEGRITY_MISMATCH` (422), `UPLOAD_EXPIRED` (409). Rationale and the eight rejected candidates: `B11_FAILURE_CATALOG.md` §2–§3 | `ADDITIVE` | three new `code` string values inside the existing envelope. **Zero new HTTP status** — every code reuses a status already in `BACKEND_API_STANDARD.md`'s closed list, and `413` was deliberately declined for that reason. Zero new envelope field | `BACKEND_ERROR_CATALOG.md` code table |
| 5 | `B11-AM-005` | `B1_AUTHORIZATION_RBAC.md` §2-§3, `BACKEND_AUTHORIZATION_MATRIX.md` | Files permission family = `file.upload`, `file.download`, with role rows already defined | Add `file.delete` and `file.manage` with the role rows in `B11_RBAC_TENANCY.md` §2. **No existing permission is renamed, widened, narrowed, or given a different role cell**; the two frozen rows are reproduced verbatim | `ADDITIVE` | two new permission codes; two new role-matrix rows. Attach/detach mint **no** code — they compose `file.upload` with the subject domain's own write permission, which is why this amendment adds two rather than four | `B1_AUTHORIZATION_RBAC.md` §2 family list and §3 matrix; `BACKEND_AUTHORIZATION_MATRIX.md` action matrix |
| 6 | `B11-AM-006` | `BACKEND_RATE_LIMIT_POLICY.md` | no row covers file upload or download | Add *"File upload \| 60/hour/workspace and 10/min/user \| workspace + user"* and *"File download \| 300/hour/workspace \| workspace + user"*, mirroring the existing "Export" and "Payment initiation" row shapes | `ADDITIVE` | two new table rows; no existing limit changed. Per that document's own closing rule these are Redis-accelerated abuse controls and are never the authority for the quota decision, which stays transactional in PostgreSQL | `BACKEND_RATE_LIMIT_POLICY.md` |
| 7 | `B11-AM-007` | `B5_MEDIA_B11_HANDOFF.md` §2 and §4 | §4: *"Enforce MIME/type/size validation and quarantine/**scanning** per its own policy \| B5 does not re-implement content safety scanning — it inherits B11's"*. §2: `checksum \| text \| populated once stored, from B11` — no algorithm stated | Two clarifications, neither changing a B5 field, flow, or decision. (a) **B11's Phase-1 content-safety policy contains no automated malware/AV scanning** (`B11-D-A024`), which is consistent with frozen `BACKEND_SECURITY_ARCHITECTURE.md`'s own "malware scanning **where available**"; the six compensating controls and the future integration point are `B11_FILE_VALIDATION.md` §8. B5's "inherits B11's" remains literally true — this records what is inherited. (b) `MessageMedia.checksum` is **SHA-256, lowercase hex, 64 characters** | `COMPATIBLE_CLARIFICATION` | no B5 schema, DTO, flow, state, or decision changes. `B5-D-A020` is untouched. Two prose cells gain a precise meaning. **Filed rather than left implied precisely because the unfiled reading — that B5 inherits a scanner — would be false** | `B5_MEDIA_B11_HANDOFF.md` §2 checksum row, §4 scanning row |
| 8 | `B11-AM-008` | `BACKEND_RECONCILIATION.md` | eight reconciliation processes are listed; none covers files or storage | Add *"Files/Storage \| `file_assets` vs provider objects \| hourly \| File service with operations coordinator"*, mirroring the existing "Scraping/provider execution" row's service-plus-coordinator shape. The eight mismatch classes, their precedence rules, and their auto/manual split are `B11_RECONCILIATION_MODEL.md` §3 | `ADDITIVE` | one new table row. The document's doctrine paragraph is reused verbatim and unmodified, including its demand for a documented precedence rule, which §3 supplies | `BACKEND_RECONCILIATION.md` |
| 9 | `B11-AM-009` | `B1_API_DTO_CONTRACTS.md` line 308 | *"`409 CONFLICT` always carries a `details.reason` from this **closed set**: `invitation_pending`, `membership_removed`, `last_workspace`, `last_active_membership`. A `409 CONFLICT` without a `reason` is invalid."* | Extend the closed vocabulary with `file_not_ready`, `file_attachment_present`, `file_upload_state_conflict`. **No new `code`, no new HTTP status, no change to any existing reason value** | `ADDITIVE` | three new `details.reason` string values inside an already-extensible closed vocabulary. `B2-D-B011` established the precedent for extending this exact list, and `B10-AM-008` was required to file it; B11 files it in the same pass that uses it | `B1_API_DTO_CONTRACTS.md` §"`CONFLICT` reason vocabulary" |
| 10 | `B11-AM-010` | `BACKEND_STATE_MACHINES.md` line 55 | *"FileAsset is `pending→available/quarantined/failed→archived`."* | Two additions, **no state added or renamed**: (a) two transitions — `available → quarantined` (operator hold) and `quarantined → available` (operator release), both `file.manage`-gated and reason-required; (b) register the orthogonal `storage_object_state` machine (`unwritten`/`present`/`purge_pending`/`purged`/`purge_failed`) as a **separate** machine on the same row, never a widening of the frozen five. The frozen five values, the frozen fan-out, and the frozen `archived` terminal are all unchanged | `ADDITIVE` | one sentence gains two transitions and a companion machine. Frozen `BACKEND_SECURITY_ARCHITECTURE.md` already requires "quarantine status" as an upload control, so a producer for it is a realization of that requirement rather than a new concept. **No transition out of `archived` is added, in this amendment or anywhere** | `BACKEND_STATE_MACHINES.md` FileAsset sentence |
| 11 | `B11-AM-011` | `BACKEND_DOMAIN_OWNERSHIP.md` line 27 | Files row: tables "file_assets"; commands "CreateUpload, DeleteAsset"; events "FileUploaded" | Extend three cells. Tables → `file_assets, file_attachments, workspace_storage_usage, file_reconciliation_cases`. Commands → the frozen two plus `UploadFileContent`, `FinalizeUpload`, `ImportFileFromUrl`, `AttachFile`, `DetachFile`, `ExpireUpload`, `PurgeFileObject`, `QuarantineFile`, `ReleaseQuarantinedFile`, `ReconcileFile`. Events → `FileUploaded` plus seven (`B11_COMMAND_EVENT_CATALOG.md` §2). **Writer, readers, integration, and forbidden-coupling cells unchanged** | `ADDITIVE` | three cells on one row grow. Both frozen command names and the frozen event name survive verbatim with their original meanings. Same class as `B10-AM-001`, and the same precedent by which B7 added `AutomationRunSkipped` | `BACKEND_DOMAIN_OWNERSHIP.md` Files row |
| 12 | `B11-AM-012` | `BACKEND_DATA_MODEL.md` line 25 | *"Files \| file_assets \| workspace/storage_key unique; checksum index"* — constraint-level only, no column-level DDL | Add `file_attachments`, `workspace_storage_usage`, `file_reconciliation_cases`; fully specify all four tables' columns per `B11_STORAGE_MODEL.md`. **Both frozen constraints are honored verbatim** — `UNIQUE (workspace_id, storage_key)` and `INDEX (checksum)` appear exactly as written, with the checksum index explicitly not used for deduplication | `ADDITIVE` | three new tables; column-level specification of one previously-undetailed table. No prior column-level schema existed for `file_assets` to contradict. Same class as `B10-AM-002` | `BACKEND_DATA_MODEL.md` Files table group; `BACKEND_ERD.md` |

## Totals

`CONTROLLED_AMENDMENT_COUNT = 12`. `ADDITIVE_AMENDMENTS = 10` (`#1, #2, #4, #5, #6, #8, #9, #10, #11, #12`). `COMPATIBLE_CLARIFICATIONS = 2` (`#3, #7`). `NON_ADDITIVE_AMENDMENTS = 0`. `MISSING_CONTROLLED_AMENDMENTS = 0` — every frozen-artifact change any other B11 document requires appears above; the sweep that produced `B11_CROSS_DOMAIN_CONTRACT_MATRIX.md` §1–§2 is the same sweep that produced this table, so the two are derived from one pass rather than independently recalled.

## What required no amendment

| Frozen artifact | Why untouched |
|---|---|
| `BACKEND_PUBLIC_ID_REGISTRY.md` | `FILE-` is already registered for `FileAsset` in the Files domain, workspace-scoped. **Zero new prefix** (`B11_PUBLIC_ID_REGISTRY.md`) |
| `BACKEND_INTEGRATION_BOUNDARIES.md` | the `FileStorageProvider` port name is reused verbatim, and its `outbound` direction is honored — B11 consumes no storage callback |
| `BACKEND_RETRY_POLICY.md` | the "Storage failure · yes · 5 · failed asset + retry action" row is reused verbatim for the purge worker and reconciliation scans |
| `BACKEND_TIMEOUT_POLICY.md` | the "Hostinger storage · 5s · 60s · 10m" row is reused verbatim; B11 proposes no different number |
| `BACKEND_FAILURE_MATRIX.md` | the existing "Storage upload failure" row is realized, not modified |
| `BACKEND_TEST_STRATEGY.md` | "upload policy" is already a mandatory category; B11 adds instances, not categories |
| `BACKEND_OPERATIONS_OBSERVABILITY.md` | "storage failures" is already a named alert class; B11 binds signals to it |
| `BACKEND_SECURITY_ARCHITECTURE.md` | every clause of its upload paragraph is realized, including "malware scanning **where available**," which is what makes `B11-D-A024` compliant rather than a deviation |
| `B1_AUTHORIZATION_RBAC.md` §4 line 169 | its "single-use, short-lived, bound to the resolved workspace" requirement for `FileAsset` download URLs is **satisfied**, not amended — B11's ticket design was built to it |
| **B8's `quota_definitions`** | **no sixth metric is filed.** Storage entitlement remains B8's decision to make or decline (`B11-D-B001`); filing it would have meant inventing per-plan allowances with no product input and no frontend evidence |
| `B2_DOMAIN_OWNERSHIP.md` / `B2_LEAD360_READ_MODEL.md` | their "File attachments out of Phase-1 CRM scope" is **honored**, not overridden; no CRM subject type is registered |
| `B5` (all other documents) | `B5-D-A020`, the `MessageMedia` field set, both media flows, and B5's five stated requirements are consumed exactly as written. Only the two prose cells in `#7` are clarified |
| `B10` (all documents) | B11 declares a subject type and a retention class **without** touching B10's format gate (`B10-D-B001`) or its retention gap (`B10-D-B004`), and cannot delete a `legal`-class file at all |
