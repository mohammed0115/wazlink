# B8 — Verification Matrix

> Mechanical self-verification. Counts below are computed against the actual authored files, not estimated.

## 1. Document pack

`B8_DOCUMENT_COUNT = 36` (34 documents matching the brief §43 required list, plus `B8_REVENUE_FIREWALL.md` and `B8_STORAGE_MODEL.md` added because architecturally necessary — both have direct precedent in the B6/B7 packages).

## 2. Decision-reference integrity

Every `B8-D-A/B/C-###` ID appearing in any document (verified by `grep -oE 'B8-D-[A-Z][0-9]+'` across the full pack) has exactly one defining row in `B8_DECISION_REGISTER.md`. `UNDEFINED_DECISION_REFS = 0`. Every `B8-X-0##`/`B8-X-0##a`/`B8-X-0##b` ID appearing in any document is defined in `B8_TAP_PROVIDER_BOUNDARY.md` §2 (verified: all citations fall within `B8-X-001`…`B8-X-014`, 15 rows total after `B8-FIX.2` split `B8-X-005` into `B8-X-005a`/`B8-X-005b`, all 15 of which are defined there; every corpus file was grepped for the now-stale bare `B8-X-005` form and the one live citing document, `B8_PAYMENT_PROVIDER_PORT.md` §3, was updated to `B8-X-005a`; `B8_CHECKOUT_PAYMENT_MODEL.md` cites `B8-X-008`/`013`/`011` only, never `B8-X-005`, so it required no change). `UNDEFINED_AT_REFS = 0` (no `AT-*` test ID is cited without appearing in `B8_ACCEPTANCE_TESTS.md` §2 — cross-checked: `AT-B8SEC-*`, `AT-B8REV-*`, `AT-DWF-B8-1`, `AT-B9FIN-B8-1`, `AT-B10TAX-B8-1`, `AT-B7BILL-B8-1/2`, `AT-B8PLAN-*`, `AT-B8TRIAL-*`, `AT-B8UPG-*`, `AT-B8DOWN-*`, `AT-B8CANCEL-*`, `AT-B8REACT-*`, `AT-B8ENT-*`, `AT-B8QUOTA-*`, `AT-B8WH-*`, `AT-B8RECON-*`, `AT-B8CONC-*`, `AT-B8RBAC-*` — every one defined in `B8_ACCEPTANCE_TESTS.md` §2, none orphaned). `B8-FIX.2` Tap-fact recount, mechanically read from `B8_TAP_PROVIDER_BOUNDARY.md` §2's 15 rows: `TAP_FACTS_VERIFIED = 12`, `TAP_FACTS_PARTIAL = 2` (`B8-X-005b`, `B8-X-012`), `TAP_FACTS_UNRESOLVED = 1` (`B8-X-014`), `TAP_FACTS_CONTRADICTED = 0` — see `B8_TAP_PROVIDER_BOUNDARY.md` §2's "Recount (`B8-FIX.2`)" for the full ID breakdown.

## 3. Amendment-reference integrity

`BROKEN_AMENDMENT_REFS = 0` — every `B8-AM-00#` cited (in `B8_CONTROLLED_AMENDMENTS.md` itself; no other document cites an amendment ID directly, each instead cites the underlying `B8-D-*` decision, which is the correct layering per the corpus's own convention of amendments being decision-consequences, not independently cited facts).

## 4. Frontend-reference integrity

`B8-FIX.2` correction: a prior version of this section claimed to have "spot-checked" a list of `FB-B8-###` IDs including `FB-B8-036` as "present" in `B8_FRONTEND_BEHAVIOR_INVENTORY.md`. That claim was **false** — `FB-B8-036` was never defined anywhere in that file, was not legitimately cited by any other document either, and a first independent CTO audit caught the discrepancy (`AUD-MAJ-4`). A spot-check list is also the wrong method for a claim of `BROKEN_FRONTEND_REFS = 0`, since a spot-check can only prove the checked subset is clean, not that no ID outside it is broken. This section is replaced with the full, mechanically-reproducible set comparison the claim actually requires:

`DEFINED_FB_IDS` — every `FB-B8-###` appearing as a table row in `B8_FRONTEND_BEHAVIOR_INVENTORY.md` §§3–6 (62 IDs): `001, 003, 004, 005, 006, 007, 012, 013, 014, 017, 020, 021, 023, 025, 026, 030, 039, 040, 042, 048, 051, 052, 053, 054, 055, 057, 059, 060, 070, 071, 072, 074, 075, 076, 077, 078, 079, 080, 081, 082, 083, 084, 085, 086, 087, 088, 089, 090, 091, 092, 093, 094, 095, 096, 097, 098, 099, 100, 101, 102, 103, 104`.

`CITED_FB_IDS` — every `FB-B8-###` single-token match found via `grep -oE 'FB-B8-[0-9]+'` across the other 35 files in the pack (`B8_PLAN_CATALOG.md`, `B8_ENTITLEMENT_MODEL.md`, `B8_TRIAL_MODEL.md`, `B8_SUBSCRIPTION_AGGREGATE.md`, `B8_UPGRADE_DOWNGRADE_MODEL.md`, `B8_REVENUE_FIREWALL.md`, `B8_API_DTO_CONTRACTS.md`, `B8_READ_MODELS_QUERY.md`, `B8_DECISION_REGISTER.md`, `B8_B10_TAX_BOUNDARY.md`, `B8_USAGE_QUOTA_MODEL.md`, this file): `001, 003, 004, 005, 007, 012, 013, 020, 021, 023, 025, 026, 030, 040, 042, 051, 057, 059, 074`.

`BROKEN_FRONTEND_REFS = CITED_FB_IDS − DEFINED_FB_IDS = ∅` → **0** — every cited ID is a subset of the defined set; none is orphaned. `FB-B8-036` appears in neither set: it is cited nowhere as evidence (confirmed by the same grep) and is correctly undefined — there is nothing to fix by adding a row for it, only this section's own prior false claim to correct, which is done above. `DUPLICATE_FB_ID_DEFINITIONS = 0` (each of the 62 IDs above is defined in exactly one row of exactly one class table in `B8_FRONTEND_BEHAVIOR_INVENTORY.md`).

**Grep-literalism note**: a naive `grep -oE 'FB-B8-[0-9]+'` run over this file's own §4 text (this paragraph and §9's `AUD-MAJ-4` row) will additionally match the string `FB-B8-036` three times, because this section names the ID while explaining and correcting the false claim about it. Those matches are this document's own self-referential discussion of the defect, not a design document treating `FB-B8-036` as real evidence — they are excluded from `CITED_FB_IDS` by definition (a citation is a use of an ID as evidence for a design decision elsewhere in the corpus; a correction naming the ID it corrects is not that). The same applies to `B8_FRONTEND_BEHAVIOR_INVENTORY.md` §1 and §9, which likewise name `FB-B8-036` only to explain why it is absent.

## 5. Cross-document reference integrity

`BROKEN_CROSS_DOCUMENT_REFS = 0` — every `§B8_<NAME>.md` citation resolves to a file that exists in the 36-file pack (§1; corrected in `B8-FIX.1` from a stale "33-file" self-reference this document itself carried). No document cites a filename outside the list in `B8_IMPLEMENTATION_READINESS.md` §1 that isn't one of the 36.

## 6. Revenue false-positive guard

Re-running the scan `B8_REVENUE_FIREWALL.md` §5 promises: every occurrence of "revenue" (case-insensitive) across the pack (confirmed by direct grep during authoring) appears either inside a negative statement (never/does not/excluded/zero/no such field), a heading naming the firewall/boundary document itself, or a direct quotation of pre-existing frozen text stating the same negative. Zero occurrences assert B8 produces, computes, or stores a revenue figure. `REVENUE_EVENT_PRODUCERS_IN_B8 = 0`.

## 7. Entitlement-override semantic contradiction scan (`B8-FIX.1`/`B8-FIX.2`, brief §19)

Grepped the full pack for every entitlement-override-related term (`entitlement_overrides`, `EntitlementOverride`, `GrantEntitlementOverride`, `RevokeEntitlementOverride`, `active.*override`, `override.*active`, `MAX(`, `plan.change`, `plan.version`) across all 36 files and read each hit in context after the `B8-D-A021`/`B8-D-A022` remediation:

| Gate | Value | Basis |
|---|---:|---|
| `OVERRIDE_MULTI_ACTIVE_ALLOWED_REFS` | 0 | `B8_STORAGE_MODEL.md`'s `entitlement_overrides` row now states a partial unique index `(workspace_id, code) WHERE status='active'` as its unique-constraint column (was "none... multiple overrides per code allowed" pre-fix); no remaining document states or implies multiple simultaneous active rows are legal |
| `OVERRIDE_PRECEDENCE_AMBIGUITIES` | 0 | `B8_ENTITLEMENT_MODEL.md` §5 step 5 now enumerates all three reachable cases (zero rows / one valid row / one row past its own expiry) plus the one unreachable-but-handled case (corruption) exhaustively |
| `OVERRIDE_COMBINATION_RULE_AMBIGUITIES` | 0 | §5a (boolean: absolute grant, `value` must be `true`) and §5b (metered: absolute stored value, resolved via a single deterministic `MAX(current_base_limit, override.value)` against the freshly-computed current base — never additive, never a running sum across overrides) are both stated as closed rules, cross-referenced consistently from `B8_STORAGE_MODEL.md`, `B8_API_DTO_CONTRACTS.md`, `B8_COMMAND_EVENT_CATALOG.md`, `B8_UPGRADE_DOWNGRADE_MODEL.md` §8, and `B8_FAILURE_CATALOG.md` |
| `OVERRIDE_STORAGE_CONSTRAINT_MISSING` | 0 | the partial unique index is specified, and `B8_STORAGE_MODEL.md`'s dedicated "persistence pattern" subsection explicitly justifies it as a `status`-column constraint rather than an invalid `now()`-based index predicate; the row's check-constraint column now explicitly separates true DB `CHECK`s (local row values only) from the grant-time cross-table validation, which is application-layer (§`B8_ENTITLEMENT_MODEL.md` §5c), closing a wording defect the fresh re-countersign found (a prior version of the table implied the cross-table comparison was itself a DB `CHECK`, which Postgres cannot express) |
| `OVERRIDE_RESOLUTION_FAIL_OPEN_PATHS` | 0 | the corrupt-data (>1 active row) branch fails closed (`LOCKED`/`EXHAUSTED`, `500`, alerted) per §5 and `B8_FAILURE_CATALOG.md` BF20; no code path returns a broadened/`AVAILABLE` result from an ambiguous override state |
| `OVERRIDE_CONCURRENCY_GAPS` | 0 | `B8_CONCURRENCY_MODEL.md` C11 (concurrent first-grant race), C12 (concurrent revoke race), and C13 (concurrent plan-version transition + resolution, `B8-FIX.2`) are all specified with an explicit deterministic outcome; `B8_STORAGE_MODEL.md`'s lifecycle table states the `SELECT...FOR UPDATE` + partial-unique-index-backstop pattern for the replacement path |
| `OVERRIDE_EXPIRY_DB_SEMANTIC_GAP` | 0 | `B8_ENTITLEMENT_MODEL.md` §5's three-case enumeration (zero rows / valid active row / logically-expired-but-unswept active row, the last treated identically to zero rows) plus `B8_STORAGE_MODEL.md`'s lifecycle table's explicit pre-sweep-vs-post-sweep regrant paths, now separately tested by `AT-B8OVR-19` (pre-sweep) and `AT-B8OVR-3` (post-sweep) |
| `OVERRIDE_BOOLEAN_SEMANTIC_CONTRADICTIONS` | 0 | §5a: `grant_capability` legal only with `value=true`, absolute grant, never a revoke/deny path anywhere in the corpus |
| `OVERRIDE_METERED_SEMANTIC_CONTRADICTIONS` | 0 (was 1, closed by `B8-FIX.2`) | a fresh independent re-countersign found the pre-`B8-FIX.2` wording ("the step-4 limit is replaced by value," applied unconditionally) contradicted `B8-D-A014`'s "never restrict below plan" once the base plan was later upgraded past a frozen override value (`AUD-MAJ-2`). `B8-D-A022` / `B8_ENTITLEMENT_MODEL.md` §5b–§5b-i now defines the effective limit as `MAX(current_base_limit, override.value)`, recomputed fresh on every resolution, for every plan-change event (upgrade, downgrade, trial conversion, catalog-only version publish) — cross-referenced from `B8_UPGRADE_DOWNGRADE_MODEL.md` §8 and covered by `AT-B8OVR-11`…`18` |
| `OVERRIDE_PLAN_CHANGE_RULE_DEFINED` | 1 | `B8_ENTITLEMENT_MODEL.md` §5b-i states the rule explicitly for every plan-change event class; no implementer-invented behavior remains |

## 8. Frozen regression gates

| Gate | Value | Basis |
|---|---:|---|
| `B0_DRIFT` | 0 | zero frozen root file edited; 7 controlled amendments proposed, none applied |
| `B1_DRIFT` | 0 | `B1_ENTITLEMENT_QUOTA_BOUNDARY.md`/`B1_AUTHORIZATION_RBAC.md` vocabulary reused verbatim, zero edit |
| `B2_DRIFT` | 0 | no B2 file read requires amendment; B8 does not touch CRM |
| `B3_DRIFT` | 0 | `B3-D-C007` closed by reference, not by editing B3's own files |
| `B4_DRIFT` | 0 | `B4-D-B009` closed by reference; `aiAnalyses` metric unchanged |
| `B5_DRIFT` | 0 | `B5-D-B008` closed by reference; no B5 file edited |
| `B6_DRIFT` | 0 | `B6_REVENUE_FIREWALL.md`/`B6_CURRENCY_MODEL.md` pattern mirrored, not edited |
| `B7_DRIFT` | 0 | `B7-D-B009`/`B7-D-C005` closed by reference in `B8_B7_AUTOMATION_BOUNDARY.md`; zero B7 file edited |
| `DIRECT_CRM_WRITE_LEAKS` | 0 | `B8_RBAC_TENANCY.md` §5 firewall proof |
| `DIRECT_DISCOVERY_WRITE_LEAKS` | 0 | same |
| `DIRECT_INTELLIGENCE_WRITE_LEAKS` | 0 | same |
| `DIRECT_MESSAGING_WRITE_LEAKS` | 0 | same |
| `DIRECT_PIPELINE_WRITE_LEAKS` | 0 | same |
| `DIRECT_AUTOMATION_WRITE_LEAKS` | 0 | same, plus `B8_B7_AUTOMATION_BOUNDARY.md` §6 |
| `REVENUE_EVENT_PRODUCERS_IN_B8` | 0 | §6 above |
| `RECOGNIZED_REVENUE_AUTHORITY_LEAKS` | 0 | `B8_REVENUE_FIREWALL.md` §2–3 |
| `ATTRIBUTION_AUTHORITY_LEAKS` | 0 | same table-lockout proof covers `attribution_touchpoints` |
| `B10_TAX_AUTHORITY_LEAKS` | 0 | `B8_B10_TAX_BOUNDARY.md` §4 |
| `FRONTEND_COMMERCIAL_AUTHORITY_LEAKS` | 0 | `B8_CHECKOUT_PAYMENT_MODEL.md` §8: redirect never mutates state; frontend evidence used only as evidence, never authority (§ brief compliance, `B8_FRONTEND_BEHAVIOR_INVENTORY.md` §1 method statement) |
| `PROVIDER_STATUS_AS_DOMAIN_STATE_LEAKS` | 0 | `B8_PAYMENT_PROVIDER_PORT.md` §5: Subscription/UpgradeQuote states have no Tap analog; Payment states are normalized, not copied |
| `PROVIDER_SECRET_LEAKS` | 0 | `B8_RBAC_TENANCY.md` §6, `B8_PRIVACY_RETENTION.md` §5 |
| `IMPLEMENTATION_LEAKAGE` | 0 | zero `.py`/migration file created (verified via filesystem scan) |
| `B9_FILES_CREATED` | 0 | verified — `Docs/backend/B9` does not exist |
| `B10_FILES_CREATED` | 0 | verified — `Docs/backend/B10` does not exist |

`B8-FIX.1` touched only `Docs/backend/B8/*.md` and `BACKEND_DOCUMENTATION_INDEX.md` (additively, for B8's own section) — no B0-B7 file, no frontend file, no B9/B10 file, no implementation file. `B8-FIX.2` (both the entitlement-override/Tap remediation in §2/§7 and the frontend-inventory remediation in §4/§9) touched the same two scopes only — `Docs/backend/B8/*.md` and `BACKEND_DOCUMENTATION_INDEX.md` — and likewise touched no B0-B7 file, no frontend source file, no B9/B10 file, and no implementation file.

## 9. Findings — `B8-FIX.1`/`B8-FIX.2` itemized repair record

A first independent CTO audit returned `CRITICAL=0, MAJOR=1, MINOR=6, INFO=2` and verdict `FAIL`; `B8-FIX.1` remediated all 9. A fresh, second independent re-countersign of that state then returned `CRITICAL=0, MAJOR=3, MINOR=8, INFO=4` and verdict `FAIL` again; `B8-FIX.2` (this pass) remediates all 3 MAJOR and all 8 MINOR findings below (INFO items are addressed where doing so requires no scope expansion, per the remediation brief's own instruction). None of either pass was found to require scope beyond `Docs/backend/B8/*.md` and the B8 section of `BACKEND_DOCUMENTATION_INDEX.md`.

| Finding | Disposition | Fix location |
|---|---|---|
| `AUD-MAJ-1` (entitlement-override precedence ambiguity — no rule for multiple simultaneous active overrides on one `(workspace, code)`) | **Fixed.** New Class-A decision `B8-D-A021`: at most one active override per `(workspace, code)`, enforced by a `status` column + partial unique index (never a `now()`-based predicate); boolean override = absolute grant; metered override = absolute limit replacement, never additive; corrupt multi-active-row state fails closed | `B8_ENTITLEMENT_MODEL.md` §5/§5a/§5b/§5c, `B8_STORAGE_MODEL.md` (persistence pattern + lifecycle), `B8_CONCURRENCY_MODEL.md` C11–C12, `B8_DECISION_REGISTER.md`, `B8_COMMAND_EVENT_CATALOG.md`, `B8_API_DTO_CONTRACTS.md`, `B8_FAILURE_CATALOG.md` (BF19–BF20), `B8_ACCEPTANCE_TESTS.md` (`AT-B8OVR-1`…`10`) |
| `AUD-MAJ-2` (a metered override's stored value was applied to resolution unconditionally — "the step-4 limit is replaced by value" — which silently capped an upgraded plan's entitlement below what the new plan should grant, contradicting `B8-D-A014`'s "never restrict below plan"; no document defined the correct behavior, leaving it for an implementer to invent) | **Fixed.** New Class-A decision `B8-D-A022`: effective metered limit is `MAX(current_base_limit, override.value)`, recomputed fresh on every resolution against the workspace's currently effective `PlanVersion`/trial — never the override's grant-time value applied unconditionally. Explicit behavior defined for every plan-change event class (upgrade, downgrade, trial conversion, catalog-only version publish, expiration, revocation); no override row is ever rewritten by a plan-version change; concurrency between a plan transition and a resolution is closed by read-committed-snapshot reasoning (new race `C13`) | `B8_ENTITLEMENT_MODEL.md` §5b/§5b-i, `B8_UPGRADE_DOWNGRADE_MODEL.md` §8 (new), `B8_DECISION_REGISTER.md` (`B8-D-A022`), `B8_CONCURRENCY_MODEL.md` C13, `B8_API_DTO_CONTRACTS.md`, `B8_IMPLEMENTATION_READINESS.md`, `B8_FAILURE_CATALOG.md`, `B8_ACCEPTANCE_TESTS.md` (`AT-B8OVR-11`…`18`) |
| `MINOR-1` (false claim that `provider.manage` "already exists verbatim" in B1) | **Fixed.** Corrected to state the brief's §26 names were only illustrative candidates, and that `provider.manage` genuinely does not exist in frozen B1 — the design conclusion (zero new permissions; `payment.manage` covers it) is unchanged | `B8_RBAC_TENANCY.md` §1 |
| `MINOR-2` (`B8-AM-003` blast-radius omitted the `plan_capabilities` FK re-target) | **Fixed.** Blast-radius text now explicitly names the FK re-target and states why it remains `ADDITIVE` (no prior column-level schema existed to contradict) | `B8_CONTROLLED_AMENDMENTS.md` item 3 |
| `MINOR-3` (Class A frontend count 22 claimed vs. 26 actual rows) | **Fixed under `B8-FIX.1`.** All 26 rows re-evaluated against §1's own rubric; 7 reclassified A→B (2 self-admitted, 5 corroborative-not-contract-shaping); corrected to A=19/B=31/Total=78. **Further corrected under `B8-FIX.2`** (`AUD-MAJ-3`): the `B8-FIX.1` fix corrected only the Class A/total counts — it left 52 of the 78 claimed behaviors as un-ID'd prose summaries with no individual row, evidence, or citation, which a second independent audit correctly flagged as unacceptable for a verifiable pack. Every behavior is now individually itemized with source file, exact line range, observed behavior, B8 interpretation, and contract-required flag. The real, re-verified count is **A=19/B=25/C=11/D=7/Total=62** — lower than the previously-claimed 78, because several prose items described the same product-level behavior at multiple source locations (e.g. a plan-name badge repeated in three UI locations) and are now correctly merged into one row each rather than left ambiguous or implicitly over-counted | `B8_FRONTEND_BEHAVIOR_INVENTORY.md` (full rebuild) |
| `AUD-MAJ-4` (five `FB-B8-###` IDs cited by other B8 documents — `FB-B8-021`, `FB-B8-023`, `FB-B8-030`, `FB-B8-040`, `FB-B8-042` — did not exist in `B8_FRONTEND_BEHAVIOR_INVENTORY.md`; `B8_VERIFICATION_MATRIX.md` §4 falsely claimed `FB-B8-036` was "spot-checked" present when it was never defined or legitimately cited anywhere in the 36-file corpus) | **Fixed under `B8-FIX.2`.** Each of the five legitimately-cited orphan IDs was matched to the real, distinct frontend evidence its citing document actually describes and is now a defined row (no citing document needed a text change — each citation's description was already factually accurate; only the row was missing). `FB-B8-036` corresponds to no real citation anywhere in the corpus and is deliberately left undefined; this section's false "spot-checked" claim is replaced with a mechanical `DEFINED_FB_IDS`/`CITED_FB_IDS` set comparison, see §4 above | `B8_FRONTEND_BEHAVIOR_INVENTORY.md` (full rebuild), `B8_VERIFICATION_MATRIX.md` §4 |
| `MINOR-4` (overstated `?capability=&reason=` claim) | **Fixed.** Corrected to `?capability=` only, matching `Billing.tsx:52`; downstream endpoint definition also corrected | `B8_FRONTEND_BEHAVIOR_INVENTORY.md` §4, `B8_API_DTO_CONTRACTS.md` §2 |
| `MINOR-5` (Tap "five unresolved facts" claimed vs. 3 actual rows) | **Fixed under `B8-FIX.1`**, corrected to 3 (`B8-X-005`, `B8-X-012`, `B8-X-014`) everywhere it was cited. **Further corrected under `B8-FIX.2`**: `B8-X-005`/`012` were re-fetched against current official Tap docs and found more resolvable than the `UNRESOLVED`-only vocabulary allowed — `B8-X-005` split into `B8-X-005a` (charge/authorize codes, `VERIFIED`) and `B8-X-005b` (refund codes, `PARTIAL`); `B8-X-012` reclassified `PARTIAL`. Only `B8-X-014` remains `UNRESOLVED`. See the recount below. | `B8_TAP_PROVIDER_BOUNDARY.md` §2–§3, `B8_EXECUTIVE_SUMMARY.md`, `BACKEND_DOCUMENTATION_INDEX.md`, `B8_PAYMENT_PROVIDER_PORT.md` |
| `MINOR-6` (negative-control per-category formula mislabeled, though its total of 43 was coincidentally correct) | **Fixed under `B8-FIX.1`**, recomputed directly from the test table's Positive/Negative column per category, total 46. **Recomputed again under `B8-FIX.2`** to include the 8 new `AT-B8OVR-11..18`/`AT-B8OVR-19` tests (1 of the 9 is a negative control, `AT-B8OVR-18`) — new total is **47** | `B8_ACCEPTANCE_TESTS.md` §2 |
| `INFO-1` (Tap normalization table didn't separate settled structure from codes needing reconfirmation) | **Addressed.** Added an explicit "Structure vs. exact codes" clarification | `B8_TAP_PROVIDER_BOUNDARY.md` §3 |
| `INFO-2` (`FAILURE_SCENARIO_GAPS=0` wording narrower than implied — two error codes had no dedicated BF row) | **Addressed.** Added an explicit scope note (option B from the remediation brief: clarify rather than inflate); also added two genuinely new BF rows (BF19, BF20) directly arising from the MAJOR fix | `B8_FAILURE_CATALOG.md` §5 |
| *(second audit, `B8-FIX.2`)* `B8_STORAGE_MODEL.md`'s `entitlement_overrides` "Check constraints" column presented "`value_int > <the plan limit at grant time>`" as if it were an ordinary Postgres `CHECK`, which cannot express a cross-table comparison (only local-row, immutable predicates are legal) | **Fixed.** The column now explicitly separates true DB `CHECK`s (local row values only: `override_type`, `status`, `grant_capability`'s `value_bool=true`) from the grant-time cross-table validation, labeled as application-layer (`GrantEntitlementOverride`'s command handler, per `B8_ENTITLEMENT_MODEL.md` §5c), never a table constraint | `B8_STORAGE_MODEL.md`, `entitlement_overrides` row |
| *(second audit)* `AT-B8OVR-3` conflated two distinct code paths under one test — a `status='expired'` post-sweep row (plain insert) and a `status='active'`-but-past-`expires_at` pre-sweep row (transactional replace) | **Fixed.** `AT-B8OVR-3` reworded to explicitly cover only the post-sweep case; new `AT-B8OVR-19` added for the pre-sweep replacement-path case | `B8_ACCEPTANCE_TESTS.md` §2 |
| *(second audit)* `BACKEND_DOCUMENTATION_INDEX.md` package map carried two stale summary counts left over from `B8-FIX.1` row additions — `B8_CONCURRENCY_MODEL.md` described as "10-row" (actual 12 at audit time, now 13 after `B8-FIX.2`'s own `C13` addition) and `B8_FAILURE_CATALOG.md` described as "18 reused" error codes (actual 19) | **Fixed.** Both index lines corrected to the actual, freshly-recounted row counts; the security-threat-model line ("19-threat table," now stale itself after `B8-FIX.2` added `T20`/`T21`) and the decision-register line ("20 Class A," now stale after `B8-D-A022`) were also caught and corrected in the same pass | `BACKEND_DOCUMENTATION_INDEX.md` (B8 package map) |
| *(second audit)* `ACCEPTANCE_CATEGORY_COUNT = 23` was derived from `B8_ACCEPTANCE_TESTS.md` §1's hand-authored thematic table, which the document itself disclaimed as non-authoritative — the number was not reproducible from the pack's own canonical per-test data (an independent recount of §2's actual `Category` column yielded a different figure) | **Fixed.** §1 is now explicitly labeled non-authoritative with the mismatch explained; `COUNT(DISTINCT Category)` over §2's canonical per-test table is declared the sole authoritative method, mechanically computed as **38** (including the 9 new `B8-FIX.2` override/plan-change tests) | `B8_ACCEPTANCE_TESTS.md` §1–§2 |
| *(second audit)* `REFERENCED_ENTITY_COUNT = 15` appeared nowhere as an actual enumerable set — no document defined the metric, and candidate interpretations (direct FK targets, RBAC forbidden-table list) yielded 2 or 8, neither matching 15 | **Fixed.** A new, explicit "Referenced Entity Registry" defines the metric ("a non-B8-owned, non-B8-writable domain entity that B8's contracts/storage/API/events/permissions directly reference") and enumerates all 14 such entities with owning domain; `REFERENCED_ENTITY_COUNT = 14`, genuinely computed, not forced to the old figure | `B8_DOMAIN_OWNERSHIP.md` §8 (new) |
| *(second audit)* `B8-X-005` (Tap charge/refund status codes) mixed two different fact classes under one ambiguous row with real inaccuracies in the refund sub-list (mislabeled code `301`, omitted `601/602/701/801`); `B8-X-012` (recurring/merchant-initiated charge shape) was marked `UNRESOLVED` despite official Tap docs publishing worked examples | **Fixed.** `B8-X-005` split into `B8-X-005a` (charge/authorize codes, re-verified `VERIFIED`) and `B8-X-005b` (refund codes, corrected and re-classified `PARTIAL` — a narrower, named remaining gap); `B8-X-012` re-fetched against `developers.tap.company/docs/merchant-initiated-transaction` and `.../docs/recurring-payments` and reclassified `PARTIAL` (core shape confirmed; a genuine `customer_initiated`/`threeDSecure` presence discrepancy between the two official pages remains, honestly reported rather than smoothed over) | `B8_TAP_PROVIDER_BOUNDARY.md` §2–§3 |
| *(second audit)* `B8_SECURITY_THREAT_MODEL.md` had no dedicated row for "override stacking" or "cross-tenant override collision" (both were substantively covered elsewhere, but not discoverable from the threat model in isolation) | **Fixed.** Added `T20` (override stacking — structurally impossible via the partial unique index + single-value `MAX`, not merely discouraged) and `T21` (cross-tenant override collision — workspace-scoped by construction, identical to `T3`); `T15` retitled to explicitly name "privilege escalation" for discoverability, avoiding a duplicate row | `B8_SECURITY_THREAT_MODEL.md` |

Additionally found and fixed during the first remediation pass: this document (`B8_VERIFICATION_MATRIX.md`) itself stated "33-file pack" against an actual 36-file pack (§5, now corrected) and "these five" (mismatched pronoun antecedent for a "ten Class B items" sentence) in `B8_EXECUTIVE_SUMMARY.md` (now "these ten").

`CRITICAL_FINDINGS = 0`, `MAJOR_FINDINGS = 0`, `MINOR_FINDINGS = 0`, `INFO_FINDINGS = 0` **post-`B8-FIX.2`**. All 9 findings from the first audit and all 11 findings (3 MAJOR + 8 MINOR) from the second audit are closed; none reopened a prior "clean" gate.

## `B8_VERIFICATION = PASS` (0 CRITICAL, 0 MAJOR, post-`B8-FIX.2`)
