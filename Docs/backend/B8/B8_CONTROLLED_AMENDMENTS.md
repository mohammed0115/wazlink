# B8 — Controlled Amendments

> Every item below requires CTO approval before implementation; B8 applies none of them. `NON_ADDITIVE_AMENDMENTS = 0`.

| # | AMENDMENT_ID | FROZEN_SOURCE | FROZEN_TEXT/SEMANTIC | B8_REQUIREMENT | CLASSIFICATION | BLAST_RADIUS | REQUIRED_UPDATES |
|---|---|---|---|---|---|---|---|
| 1 | `B8-AM-001` | `BACKEND_DOMAIN_OWNERSHIP.md` | Both the "Entitlements" row (aggregate `Subscription`) and the "Billing" row (aggregate `Subscription/Invoice/UpgradeQuote`) list `subscriptions` among their own authoritative tables — ambiguous dual ownership | State explicitly that `billing` is `subscriptions`' sole writer; `entitlements` is a read-only consumer; correct the Entitlements row's "authoritative tables" to read "referenced tables: `subscriptions` (read-only)" | `COMPATIBLE_CLARIFICATION` | one table row's prose in one frozen document; no schema, DTO, or API change | `BACKEND_DOMAIN_OWNERSHIP.md` Entitlements/Billing rows |
| 2 | `B8-AM-002` | `BACKEND_DOMAIN_OWNERSHIP.md`, `BACKEND_COMMAND_EVENT_CATALOG.md` | The Billing row names the command `InitiateUpgrade`; the fuller catalog names it `InitiatePlanUpgrade` — two frozen documents disagree | Record `InitiatePlanUpgrade` as authoritative and `InitiateUpgrade` as the abbreviated/superseded spelling in the ownership matrix, realized by the frozen `CreateUpgradeQuote`→`CreatePayment` sequence | `COMPATIBLE_CLARIFICATION` | one cell's spelling; no endpoint, DTO, or behavior ever used either name directly | `BACKEND_DOMAIN_OWNERSHIP.md` Billing row |
| 3 | `B8-AM-003` | `BACKEND_DATA_MODEL.md` | Entitlements table group lists `plans, capabilities, plan_capabilities, quota_definitions, subscriptions, usage_counters, usage_ledger` with no version concept and no column-level DDL for any table in the group (only a table-group-wide constraint note, "plan/capability unique"); Billing table group names `billing_customers, subscriptions, upgrade_quotes, invoices, invoice_lines, payments, payment_attempts, refunds` with no further column detail | Add `plan_versions` and `plan_version_quotas` tables; **re-target `plan_capabilities`' foreign key from `plans` to `plan_versions`** (frozen text never specified this FK's target table at column level — it named `plan_capabilities` only as a table in the group list — so this is a first specification, not a change to a previously-stated fact); add `subscriptions.plan_version_id`, `.pending_plan_version_id`, `.trial_used_at`, `.billing_customer_id`, `.cancel_at_period_end`, `.cancelled_at`, `.version` columns; fully specify `billing_customers`/`payment_attempts`/`refunds` columns per `B8_STORAGE_MODEL.md` | `ADDITIVE` | new tables (`plan_versions`, `plan_version_quotas`); new nullable/defaulted columns on `subscriptions` (no existing column removed, retyped, or made non-nullable); **`plan_capabilities`' FK target is specified for the first time as `plan_version_id`, not redirected from an already-frozen value** — no prior column-level schema existed for this table to contradict, so no existing constraint is broken; full column-level specification of `billing_customers`/`payment_attempts`/`refunds`, none of which had prior column detail either. Affected downstream references: `B8_PLAN_CATALOG.md` §1 (plan-capability lookup now version-scoped), `B8_ENTITLEMENT_MODEL.md` §5 step 4 (reads `plan_capabilities` via `plan_version_id`), `B8_API_DTO_CONTRACTS.md` (no DTO shape change — `Plan`/`EntitlementDecision` remain as frozen) | `BACKEND_DATA_MODEL.md` Entitlements/Billing rows, `BACKEND_ERD.md` |
| 4 | `B8-AM-004` | `BACKEND_OPENAPI_V1.yaml`, `BACKEND_DTO_CONTRACTS.md` | `EntitlementDecision` schema is closed (`additionalProperties: false`) with exactly `{capability, status, allowed, usage?, remaining?, target_plan_ref?, evaluated_at}`; `UsageDTO` is `{capability, used, limit?, remaining?, period}` | Add optional, nullable `reset_at`, `source`, `reason` to `EntitlementDecision`; add optional, nullable `reset_at` to `UsageDTO` | `ADDITIVE` | two schema objects gain optional fields; no existing field renamed, removed, or made required; no existing consumer's request/response shape breaks | `BACKEND_OPENAPI_V1.yaml` `EntitlementDecision`/`UsageDTO` schemas, `BACKEND_DTO_CONTRACTS.md` |
| 5 | `B8-AM-005` | `BACKEND_ERROR_CATALOG.md` | Closed code list ends at `INTERNAL_ERROR`; no Billing-lifecycle-specific codes beyond the four `QUOTE_*` rows exist | Add `PLAN_RETIRED` (422), `SUBSCRIPTION_TRANSITION_INVALID` (409), `DOWNGRADE_BLOCKED` (409, reserved/unreachable in Phase 1), `RECONCILIATION_MISMATCH` (409), `PROVIDER_CONFIGURATION_INVALID` (409), `ENTITLEMENT_OVERRIDE_INVALID` (422) | `ADDITIVE` | six new `code` string values inside the existing envelope/status doctrine; zero new HTTP status, zero new envelope field, mirroring the `QUOTE_*` precedent exactly | `BACKEND_ERROR_CATALOG.md` |
| 6 | `B8-AM-006` | `BACKEND_RATE_LIMIT_POLICY.md` | "Payment initiation \| 10/hour/workspace and 3/min/user" is the only Billing-specific row; no row covers subscription-lifecycle commands (cancel/reactivate/downgrade) or entitlement-override grants | Add "Subscription lifecycle commands (cancel/reactivate/downgrade) \| 20/hour/workspace \| workspace + user" and "Entitlement override grant \| 30/hour/operator \| operator" (mirroring the existing "Admin repair" row's shape) | `ADDITIVE` | two new table rows; no existing row's limit changed | `BACKEND_RATE_LIMIT_POLICY.md` |
| 7 | `B8-AM-007` | `BACKEND_API_CATALOG.md` | Catalog lists only the six already-frozen Billing/Entitlements operations (`/plans`, `/entitlements`, `/usage`, `/billing/upgrade-quotes`, `/billing/payments`, `/billing/invoices`) | Add the nine new B8 operations (§`B8_API_DTO_CONTRACTS.md` §2) to the catalog table; confirm none receives the `filters`/`sort` markers (unchanged allow-list, still only `deals` and `billing/invoices`) | `ADDITIVE` | nine new table rows; zero existing row modified; the explicit filtering/sorting allow-list sentence is unchanged, not reopened | `BACKEND_API_CATALOG.md`, `BACKEND_OPENAPI_V1.yaml` (new paths/operationIds/schemas) |

## Totals

`CONTROLLED_AMENDMENT_COUNT = 7`. `ADDITIVE_AMENDMENTS = 5` (`#3, #4, #5, #6, #7`). `COMPATIBLE_CLARIFICATIONS = 2` (`#1, #2`). `NON_ADDITIVE_AMENDMENTS = 0`. `MISSING_CONTROLLED_AMENDMENTS = 0` — every frozen-artifact change any other B8 document requires is listed above; no B8 document assumes an amendment not itemized here.

## What required no amendment

Zero new public-ID prefix (§`B8_DOMAIN_OWNERSHIP.md`, `BACKEND_PUBLIC_ID_REGISTRY.md` unchanged). Zero new RBAC permission (`B8-D-A015`, `B1_AUTHORIZATION_RBAC.md` unchanged). Zero change to any frozen state machine's state names or edges (`BACKEND_STATE_MACHINES.md` unchanged — B8 only adds interpretive transition-authority detail, which is additive documentation, not a frozen-file edit, since it lives entirely in `B8_SUBSCRIPTION_STATE_MACHINE.md`). Zero change to any frozen command/event name (B8 reuses `CreateUpgradeQuote`, `CancelUpgradeQuote`, `CreatePayment`, `ProcessPaymentWebhook`, `ReconcilePayment`, `UpgradeQuoteIssued`, `UpgradeQuoteConsumed`, `UpgradeQuoteExpired`, `PaymentSucceeded`, `PaymentFailed`, `SubscriptionActivated`, `InvoiceIssued` verbatim). Zero ADR renumbering (B8's own decisions use the phase-scoped `B8-D-*` convention, not root `ADR-*` numbers, matching B1–B7's own precedent).
