# B10 — Controlled Amendments

> Every item below requires CTO approval before implementation; B10 applies none of them. `NON_ADDITIVE_AMENDMENTS = 0`.

| # | AMENDMENT_ID | FROZEN_SOURCE | FROZEN_TEXT/SEMANTIC | B10_REQUIREMENT | CLASSIFICATION | BLAST_RADIUS | REQUIRED_UPDATES |
|---|---|---|---|---|---|---|---|
| 1 | `B10-AM-001` | `BACKEND_DOMAIN_OWNERSHIP.md` | Tax row lists tables as "tax_invoices, lines, submissions" (shorthand) with no `legal_entities`/`tax_profiles`/`tax_buyer_profiles`/backlog table anticipated | Expand the table list to `tax_invoices, tax_invoice_lines, tax_submissions, legal_entities, tax_profiles, tax_buyer_profiles, pending_tax_document_classifications` (`B10-FIX.1` adds the seventh); writer/command/event/integration/forbidden-coupling cells unchanged | `ADDITIVE` | one row's table-list cell; no schema, DTO, or API change implied by the row itself | `BACKEND_DOMAIN_OWNERSHIP.md` Tax row |
| 2 | `B10-AM-002` | `BACKEND_DATA_MODEL.md` | Tax table group lists `tax_invoices, tax_invoice_lines, tax_submissions` with only unique-constraint-level detail ("invoice relation unique; provider reference unique"), no column-level DDL | Add `legal_entities`, `tax_profiles`, `tax_buyer_profiles`, and (`B10-FIX.1`, new) `pending_tax_document_classifications` tables; fully specify all seven tables' columns per `B10_STORAGE_MODEL.md` | `ADDITIVE` | new tables; full column-level specification of four previously-undetailed tables — no prior column-level schema existed for any of them to contradict | `BACKEND_DATA_MODEL.md` Tax table group, `BACKEND_ERD.md` |
| 3 | `B10-AM-003` | `BACKEND_DTO_CONTRACTS.md` | Frozen `TaxInvoice` DTO: `public_id, invoice_ref, seller/buyer refs, VAT fields, totals, UUID, QR payload, ZATCA status` — architecture-level only, never bound to a shipped OpenAPI schema | Realize `invoice_ref` as `source_type`/`source_ref` (polymorphic-shaped, mirroring B9's own `RevenueEvent.source_ref` precedent, since a credit/debit note has no single "invoice" in the same sense) plus, **corrected under `B10-FIX.1`,** two additive, independent discriminators — `document_kind` (invoice/credit_note/debit_note; renamed from the originally-proposed single `document_type`) and `invoice_classification` (standard/simplified) — and, for notes only, `reference_invoice_ref` (a second, distinct pointer at the *original tax document*, not the B8 invoice). The `B10-FIX.1` correction splits what was originally proposed as one field into two, closing `B10-AUDIT-C1`'s root architectural cause | `COMPATIBLE_CLARIFICATION` | field-naming/shape realization only; the frozen name was never bound to a shipped schema, so nothing observable breaks — same precedent as `B8-D-A002`'s `InitiatePlanUpgrade`/`InitiateUpgrade` treatment | `BACKEND_DTO_CONTRACTS.md` `TaxInvoice` row |
| 4 | `B10-AM-004` | `BACKEND_ERROR_CATALOG.md` | Closed code list already includes `TAX_VALIDATION_REQUIRED`; no other Tax/ZATCA-lifecycle-specific codes exist | Add 11 new `code` values (`TAX_APPLICABILITY_UNKNOWN`, `TAX_CONFIGURATION_MISSING`, `ZATCA_CREDENTIAL_INVALID`, `ZATCA_CERTIFICATE_EXPIRED`, `ZATCA_SUBMISSION_REJECTED`, `ZATCA_PROVIDER_UNAVAILABLE`, `ZATCA_TIMEOUT`, `ZATCA_RATE_LIMITED`, `TAX_DUPLICATE_SUBMISSION`, `TAX_DOCUMENT_STATE_CONFLICT`, and (`B10-FIX.1`, new) `TAX_CORRECTION_EXCEEDS_ORIGINAL` — full table `B10_ZATCA_FAILURE_RETRY_MODEL.md` §1, `B10_FAILURE_CATALOG.md` §2; a twelfth candidate, `TAX_VALIDATION_FAILED`, was considered and rejected under `B10-FIX.1a` as a duplicate of the already-frozen `TAX_VALIDATION_REQUIRED` reused below, `B10-CS-1`) | `ADDITIVE` | eleven new `code` string values inside the existing envelope/status doctrine; zero new HTTP status, zero new envelope field | `BACKEND_ERROR_CATALOG.md` |
| 5 | `B10-AM-005` | `B1_AUTHORIZATION_RBAC.md` §2 | Permission catalog includes `tax.view` only; no write/management-level Tax permission exists | Add `tax.manage`, `tax.applicability.manage`, `zatca.manage` (§`B10_RBAC_TENANCY.md` §1) | `ADDITIVE` | three new permission codes; zero existing permission renamed or widened; role matrix rows added, none altered | `B1_AUTHORIZATION_RBAC.md` §2–3, `BACKEND_AUTHORIZATION_MATRIX.md` |
| 6 | `B10-AM-006` | `BACKEND_RATE_LIMIT_POLICY.md` | No row covers tax-applicability changes, ZATCA submission retries, or backlog resolution | Add "Tax applicability change \| 10/day/operator \| operator", "ZATCA submission retry (operator-invoked) \| 20/hour/operator \| operator", and (`B10-FIX.1`, new) "Pending tax classification resolve (operator-invoked) \| 10/hour/operator \| operator" (mirroring the existing "Entitlement override grant" row's shape from `B8-AM-006`) | `ADDITIVE` | three new table rows; no existing row's limit changed | `BACKEND_RATE_LIMIT_POLICY.md` |
| 7 | `B10-AM-007` | `BACKEND_API_CATALOG.md` | No Tax/ZATCA operation exists in the catalog | Add the 13 B10 operations (`B10-FIX.1` adds two backlog operations to the original 11; §`B10_API_DTO_CONTRACTS.md` §1) to the catalog table; mark `listTaxInvoices` and (`B10-FIX.1`, new) `listPendingTaxClassifications` with the `filters` marker | `ADDITIVE` | thirteen new table rows; the explicit filtering/sorting allow-list gains two entries (`tax/invoices`, `tax/pending-classifications`), consistent with `BACKEND_API_CATALOG.md`'s own "Explicit filtering and sorting markers" convention rather than silently assumed | `BACKEND_API_CATALOG.md`, `BACKEND_OPENAPI_V1.yaml` |
| 8 | `B10-AM-008` | `B1_API_DTO_CONTRACTS.md` | *"`CONFLICT` reason vocabulary. `409 CONFLICT` always carries a `details.reason` from this **closed set**: `invitation_pending`, `membership_removed`, `last_workspace`, `last_active_membership`. A `409 CONFLICT` without a `reason` is invalid."* (line 308) | Extend the closed `details.reason` vocabulary with `applicability_transition_conflict` (used by `SetTaxApplicability` when a concurrent conflicting transition is in flight, §`B10_TAX_APPLICABILITY_MODEL.md` §7, `B10_FAILURE_CATALOG.md` §1); **no new `code`, no new HTTP status, no change to any existing reason value** | `ADDITIVE` | one new `details.reason` string value inside an already-extensible closed vocabulary (B2's `B2-D-B011` is the exact precedent for extending this same list); zero existing reason value altered, zero existing operation's behavior changed | `B1_API_DTO_CONTRACTS.md` §"`CONFLICT` reason vocabulary" |

**`B10-AM-008` closes `B10-AUDIT-M1`.** The pre-`B10-FIX.1` pack used `applicability_transition_conflict` (`B10_FAILURE_CATALOG.md` §1) without registering the amendment this exact class of change requires — B2's own `B2-D-B011` had already established the precedent that extending this specific closed vocabulary needs a controlled amendment, and the original B10 pass missed it despite the precedent being one phase away.

## Totals

`CONTROLLED_AMENDMENT_COUNT = 8` (`B10-FIX.1` adds `#8`). `ADDITIVE_AMENDMENTS = 7` (`#1, #2, #4, #5, #6, #7, #8`). `COMPATIBLE_CLARIFICATIONS = 1` (`#3`). `NON_ADDITIVE_AMENDMENTS = 0`. `MISSING_CONTROLLED_AMENDMENTS = 0` — every frozen-artifact change any other B10 document requires is listed above, independently re-swept during `B10-FIX.1` for any further omission (none found beyond the one this fix closes).

## What required no amendment

Zero new public-ID prefix (`TAX-` already registered, §`B10_PUBLIC_ID_REGISTRY.md`). Zero change to `BACKEND_RECONCILIATION.md`'s existing "ZATCA \| TaxInvoice vs submission status \| hourly \| Tax service" row — B10 realizes it exactly as written, no amendment needed. Zero change to `BACKEND_INTEGRATION_BOUNDARIES.md`'s existing `TaxProvider` port name — B10 reuses it verbatim. Zero change to `BACKEND_DOMAIN_OWNERSHIP.md`'s command/event/integration/forbidden-coupling cells for the Tax row (only the table list, §amendment 1). Zero ADR renumbering (B10 uses the phase-scoped `B10-D-*` convention).
