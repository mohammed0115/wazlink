# B13 — Dependency / Supply-Chain Security

> Design only. Defines the operational policy for dependency inventory, scanning, and triage. **B13 does not modify any dependency, manifest, or lockfile.** The repository's committed package manager and lockfile are treated as the authoritative dependency baseline; where a check was not executed in this pass, that is recorded as unrun rather than assumed either way.

## 1. Current repository state — verified during this pass

| Check | Result | Evidence |
|---|---|---|
| Root `package.json` (frontend, `leadflow-scenario`) | present — 51 direct `dependencies`, 22 `devDependencies` | repository root |
| Declared package manager | **pnpm**, version-pinned with an integrity hash (`pnpm@10.4.1+sha512…`) | `package.json` `packageManager` field |
| Frontend lockfile | **present and committed** — `pnpm-lock.yaml`, `lockfileVersion: '9.0'`, ~1,400 resolved package entries | tracked in Git; present in the repository's initial commit, so it has existed for the whole of repository history |
| Lockfile enforcement in CI | **enforced** — the CI workflow installs with `pnpm install --frozen-lockfile`, which fails the build if `package.json` and `pnpm-lock.yaml` disagree | `.github/workflows/deploy-pages.yml` |
| Competing lockfiles | **none** — no `package-lock.json` and no `yarn.lock` exists anywhere in the repository | directory search during this pass |
| Backend dependency manifest (`requirements.txt`/`pyproject.toml`/`Pipfile`) | **does not exist** — no backend implementation exists yet (B0–B13 are architecture only) | directory search during this pass: no Python manifest, no `manage.py`, no `*.py` file |
| Current frontend vulnerability baseline | **not established by B13** | no package-manager audit was executed — see §1.1 |

### 1.1 What is settled, and what is deliberately left open

**Dependency versions are pinned and reproducible today.** `pnpm-lock.yaml` fixes the exact resolved version of every direct and transitive dependency, and CI's `--frozen-lockfile` install makes that resolution enforced rather than advisory. There is no missing-lockfile gap, and no frontend lockfile needs to be generated by the implementation phase.

**What is not established is the current vulnerability baseline.** B13 did not run `pnpm audit` or equivalent: a package-manager audit in this environment requires registry resolution and install-class behaviour that could mutate the working tree or the lockfile, and B13's write scope is documentation only. This pass was therefore restricted to read-only inspection.

Consequently **B13 does not claim the current dependency set is free of known vulnerabilities, and does not claim any vulnerability has been remediated.** Establishing that baseline and triaging whatever it reports is implementation/operations work, governed by §§2–4. This is an unrun check, not a repository defect.

## 2. Dependency inventory policy (forward-looking)

| Requirement | Detail |
|---|---|
| Lockfile committed | every dependency manifest has a committed lockfile. The frontend already satisfies this via `pnpm-lock.yaml` (§1); a future backend adds its own (`requirements.lock`/`poetry.lock`/equivalent) when its manifest is first created |
| Single source of resolution truth | **no competing frontend lockfile may be introduced.** Generating a `package-lock.json` or `yarn.lock` alongside `pnpm-lock.yaml` creates two conflicting resolution records and is prohibited; the frontend package manager is pnpm, as declared by `package.json`'s `packageManager` field and enforced by CI |
| Tooling follows the repository | any install, audit, or scan step must use the repository's actual package manager and lockfile (pnpm / `pnpm-lock.yaml`). A step that assumes npm or Yarn is wrong for this repository regardless of what it reports |
| Inventory visibility | a Software Bill of Materials (SBOM) or equivalent dependency-tree listing is generated at build/CI time so the exact resolved version set is always inspectable |
| Transitive dependencies | scanning covers the full resolved tree, not only direct dependencies — most real-world vulnerable-package incidents are transitive |

## 3. Vulnerability scanning

| Ecosystem | Tool class (product choice deferred to `B14`) | Cadence |
|---|---|---|
| Python (future backend) | a dependency-vulnerability scanner integrated into CI (e.g., `pip-audit`-class tooling) | every CI run against every pull request, plus a scheduled scan independent of code changes (dependencies can become vulnerable after merge, when a new CVE is disclosed) |
| Node/frontend | a dependency-vulnerability scanner run against the committed `pnpm-lock.yaml` using the repository's own package manager (`pnpm audit`-class tooling — **not** `npm audit`, which is wrong for this repository) | same cadence as above |
| Container/base image (once implementation begins) | image-scanning tooling as part of the build pipeline | every image build |

## 4. Severity triage and patch cadence

| Severity | Response |
|---|---|
| Critical/High, with a known exploited or easily exploitable path | patch within a defined short window (**PROPOSED**: 7 days, `B13-D-B026`, Class B) or apply a documented compensating control if an immediate patch is infeasible |
| Critical/High, no known exploitation path, requires a breaking upgrade | tracked, scheduled into the next planned dependency-upgrade cycle, with a documented risk-acceptance if deferred past the initial window |
| Medium/Low | addressed in routine dependency-maintenance cycles, not emergency patched |
| Abandoned package (no maintainer activity, no fix forthcoming) | evaluated for replacement; if replacement is infeasible short-term, the risk is documented and monitored, never silently ignored |

**Every deferral or risk-acceptance is a documented decision, not silence.** A dependency review log entry is required before any release that ships a known unpatched Critical/High vulnerability, naming the vulnerability, why it was not patched, and the compensating control (if any).

## 5. Container/base-image dependencies

Recorded as a forward requirement for when implementation begins: the base image is chosen for minimal attack surface (§7 of `B13_DEPLOYMENT_SECURITY.md`), scanned at every build, and rebuilt on a defined cadence even absent an application code change, because base-image vulnerabilities are disclosed independently of WazLink's own release cycle.

## 6. What B13 does not do

B13 does not install dependencies, generate or modify a lockfile, upgrade any dependency, run a package-manager audit, or otherwise modify `client/**`, `package.json`, or `pnpm-lock.yaml` — the write scope for this phase is documentation only (per the governing brief's hard repository rules). Establishing the current vulnerability baseline (§1.1) is implementation-phase work, not a B13 action.

## 7. Acceptance controls

| ID | Assertion |
|---|---|
| `AT-B13DEP-1` | Every dependency manifest has exactly one committed lockfile, and CI installs with a frozen-lockfile flag so manifest/lockfile drift fails the build. Satisfied today for the frontend (`pnpm-lock.yaml` + `pnpm install --frozen-lockfile`, with no competing `package-lock.json` or `yarn.lock`); required of a future backend manifest when it is created |
| `AT-B13DEP-2` | CI fails (or explicitly flags) a pull request that introduces a Critical-severity known-vulnerable dependency without a documented risk-acceptance |
| `AT-B13DEP-3` | A scheduled scan independent of code changes runs at least weekly against the resolved dependency tree |
| `AT-B13DEP-4` | Every documented risk-acceptance for a deferred vulnerability has an owner and a review date |
